
{% extends "::layout.html.twig" %}

{% block title %}{{ parent() }} - Entrer Résultat{% endblock %}

{% block body %}
	<h1>Entrer un résultat</h1>

{% if form is defined %}
	<form action="{{ path('bloom_match_up_entrer_resultat') }}" method="post" {{ form_enctype(form) }}>
	    {{ form_widget(form) }}
	    <input type="submit" class="btn btn-primary" />
	</form>
{% endif %}

{% if form2 is defined %}
	<form action="{{ path('bloom_match_up_entrer_resultat') }}" method="post" {{ form_enctype(form2) }}>
	    {{ form_widget(form2) }}
	    <input type="submit" class="btn btn-primary" />
	</form>
{% endif %}


{% if adversaire is defined%}
<ul>
{{adversaire}}
</ul>
{% endif %}


{% endblock %}