<!DOCTYPE HTML>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title>{% block title %}MatchUp{% endblock %}</title>
    {% block stylesheets %}
      <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}" type="text/css" />
    {% endblock %}
  </head>

  <body>
    <div class="container">
      <div id="header" class="hero-unit">
        <h1>MatchUp Bundle</h1>
        <p>Rencontrez d'autres joueurs de votre salle.</p>
      </div>

      <div class="navbar">
        <div class="navbar-inner">
          <a class="brand" href="#">Match Up</a>
          <ul class="nav">
            <li class="active"><a href="{{ path('bloom_match_up_homepage') }}">Accueil</a></li>
            <li><a href="{{ path('fos_user_security_login') }}">Connexion</a></li>
            <li><a href="{{ path('fos_user_registration_register') }}">S'inscrire</a></li>
          </ul>
        </div>
      </div>

      <div id="myCarousel" class="carousel slide">
        <ol class="carousel-indicators">
          <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
          <li data-target="#myCarousel" data-slide-to="1"></li>
          <li data-target="#myCarousel" data-slide-to="2"></li>
        </ol>
        <!-- Carousel items -->
        <div class="carousel-inner">
          <div class="active item">
              <img src="{{ asset('img/caroussel1.jpg') }}" class="img-rounded" style="margin:0px auto;" /> 
          </div>
          <div class="item">
            <img src="{{ asset('img/caroussel2.jpg') }}" class="img-rounded" style="margin:0px auto;"  />
            <div class="carousel-caption">Page 2 de présentation</div>
          </div>
          <div class="item">
              <img src="{{ asset('img/caroussel3.jpg') }}" class="img-rounded" style="margin:0px auto;"  />
              <div class="carousel-caption">Page 3 de présentation</div>
          </div>
        </div>
        <!-- Carousel nav -->
        <a class="carousel-control left" href="#myCarousel" data-slide="prev">&lsaquo;</a>
        <a class="carousel-control right" href="#myCarousel" data-slide="next">&rsaquo;</a>
      </div>

      <footer>
        <p>The sky's the limit © 2014 and beyond.</p>
      </footer>
    </div>

    {% block javascripts %}
    {# Ajoutez ces lignes JavaScript si vous comptez vous servir des fonctionnalités du bootstrap Twitter #}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js"></script>
    <script type="text/javascript" src="{{ asset('js/bootstrap.js') }}"></script>
    {% endblock %}
  </body>
</html>